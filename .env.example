# Copy to .env to develop against the tracked defaults.
#
# env=dev makes the editor and the daemon read and write data/defaults only,
# so nothing you author here reaches the profiles you actually fly, which live
# in Saved Games. Anything else, or no file at all, behaves like an install.
env=prod
