{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The editor talks only to its own commands. It opens no URLs, runs no shell, and reads no files the backend did not hand it.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
