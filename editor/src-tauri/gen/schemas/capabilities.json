{"default":{"identifier":"default","description":"The editor talks only to its own commands. It opens no URLs, runs no shell, and reads no files the backend did not hand it. The one page it can have opened, a newer release, is chosen and checked by the backend.","local":true,"windows":["main"],"permissions":["core:default"]}}